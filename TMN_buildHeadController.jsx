buildHeadControl();function buildHeadControl(){	// begin an Undo group	app.beginUndoGroup("build head control");			// check if a comp is active	    var curComp = app.project.activeItem;    if (!curComp || !(curComp instanceof CompItem))    {        alert("Please select a Composition.");        return;    }		// add a null object to the comp for the slider controls	var theController = curComp.layers.addNull();	theController.source.name = "headControl_"+curComp.name;		var theNames = new Array();	theNames[0] = "eye_lids";	theNames[1] = "eyes";	theNames[2] = "mouth";	for(var i = 0; i< theNames.length; i++){	var theSliderEffect = theController("ADBE Effect Parade").addProperty("ADBE Slider Control"); 	theSliderEffect.name = theNames[i];	}			app.endUndoGroup();    }